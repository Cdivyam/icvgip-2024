{
  "name": "icvgip-2024",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean-dist": "rm -rf dist && mkdir dist",
    "copy-assets": "cp -r src/assets/* dist",
    "init": "npm run clean-dist && npm run copy-assets",
    "start": "npm init && npx parcel src/index.html",
    "build": "npm init && npx parcel build src/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "http-server": "^14.1.1",
    "parcel": "^2.11.0",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
